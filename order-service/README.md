# Order Service

Independent Go module for customer/admin order APIs, transactional stock updates, and `order.created` Kafka events.

Set `DATABASE_URL`, `JWT_SECRET`, and `KAFKA_BROKERS`. Run `go test ./...` and `go run ./cmd/api`.

# Product Service

Independent Go module for product CRUD, filtering, pagination, Redis caching/rate limiting, and Kafka product events.

Set `HTTP_PORT`, `DATABASE_URL`, `JWT_SECRET`, `REDIS_ADDR`, and `KAFKA_BROKERS`. Run `go test ./...` and `go run ./cmd/api`.
